<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <span class="navbar-brand"><%= t('accounts') %></span>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item <%= 'active' if request.original_fullpath == '/'%>">
        <%= link_to t('home'), root_path, class: 'nav-link' %>
      </li>
      <li class="nav-item <%= 'active' if request.original_fullpath == '/transactions'%>">
        <%= link_to t('transactions'), transactions_path, class: 'nav-link' %>
      </li>
      <li class="nav-item <%= 'active' if request.original_fullpath == '/users'%>">
        <%= link_to t('users'), users_path, class: 'nav-link' %>
      </li>
    </ul>
  </div>
</nav>

<%=  %>